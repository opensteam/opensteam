<% current_user.quick_steams.sort.each_with_index do |qs,i| %>
  <%= qs.to_html %>
<% end %>

<%= render :partial => "admin/_header/drop_me", :locals => { :style => "" } %>

<%= sortable_element( :tabs,
  :ghosting => false, :tag => "a", :overlap => "horizontal", :only => "quicksteam_tab", :constraint => "horizontal",
  :url => order_admin_system_quicksteams_path, :method => :post,
  :onChange => "function(e) { e.onclick = function() { return false; } }",
  :update => :tabs
) %>
