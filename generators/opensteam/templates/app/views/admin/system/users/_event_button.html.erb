<%= link_to(
  backend_image_tag( AdminHelper.user_event_images[event_button] ),
  send_event_admin_system_user_path( user, :event => event_button.to_s ),
  :class => 'grid',
  :method => :put,
  :title => "#{event_button.to_s.humanize} User"
) %>
  